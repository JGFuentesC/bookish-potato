[project]
name = "rama-oltp"
version = "0.1.0"
description = "OLTP database for RAMA air-quality data (CDMX) + OLAP dashboard"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "polars>=1.42.1",
    "pyarrow>=25.0.0",
    "pydantic>=2.13.4",
    "psycopg[binary,pool]>=3.1.0",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
]
