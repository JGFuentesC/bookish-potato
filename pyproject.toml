[project]
name = "rama-oltp"
version = "0.1.0"
description = "OLTP database for RAMA air-quality data (CDMX)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "polars>=1.42.1",
    "pyarrow>=25.0.0",
    "pydantic>=2.13.4",
    "psycopg[binary]>=3.1.0",
]
